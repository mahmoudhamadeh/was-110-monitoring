services:
  sfp-monitor:
    build: .
    container_name: sfp_monitor_app
    restart: unless-stopped
    ports:
      - "5050:5050"
    environment:
      - SFP_ROOT_PASSWORD
    # network_mode: host
